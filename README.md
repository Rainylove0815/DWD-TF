# DWD-TF
In the co-training framework for semi-supervised medical image segmentation, when both models exhibit low confidence, a dynamically weighted ensemble pseudo-labeling strategy is employed, integrating confidence scores and entropy-based uncertainty to guide pseudo-label fusion.
